# Dependencies
node_modules/

# Environment variables
.env
.env.*
!.env.example

# Build output
dist/

# Logs
*.log

# OCR trained-data cache (should live in the OS temp dir — see
# utils/purchaseImportExtract.js — this is a safety net in case it doesn't)
*.traineddata
